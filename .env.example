SERVICE_AUTH_TOKEN=replace-with-a-service-token
TENANT_ID=tenant-acme
# Prefer an absolute path when launching from Claude Desktop or Cursor.
MCP_CONFIG_PATH=./config.json
# Optional local dashboard telemetry. Leave false unless dashboard/ is installed and built.
MCP_DASHBOARD_ENABLED=false
